<?php

if (!function_exists('url')) {
    /**
     * Generate an absolute URL.
     *
     * @param string $path
     * @return string
     */
    function url($path = '')
    {
        echo $path;
    }
}
